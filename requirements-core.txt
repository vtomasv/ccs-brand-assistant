# ============================================================
# CCS Brand Assistant — Dependencias CORE (servidor web)
# ============================================================
# Solo las dependencias esenciales para que el servidor arranque.
# Las dependencias opcionales (playwright, diffusers, torch)
# se instalan por separado en install.json.
# ============================================================

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
aiofiles>=23.2.0
requests>=2.31.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
pydantic>=2.5.0
Pillow>=10.0.0
